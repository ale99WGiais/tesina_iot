
create table object(
    uid text primary key,
    local_path text,
    size integer,
    complete integer,
    created text,
    checksum text
);

create table transfer(
    id text primary key,
    uid integer,
    dest text,
    request_time text
);

create table stats(
    storage int,
    downspeed int,
    upspeed int
);

insert into stats(storage, downspeed, upspeed) values (100000000, 50, 10);

